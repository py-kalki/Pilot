version: "3.9"

services:
  mongodb:
    image: mongo:7
    container_name: pilot-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      MONGO_INITDB_DATABASE: pilot
    restart: unless-stopped

volumes:
  mongo-data:
